mutation AssignRegistrationRole($input: AssignRegistrationRoleInput!) {
  assignRegistrationRole(input: $input) {
    registration {
      id
      roles {
        id
        name
      }
    }
  }
}
