query FetchEventRegistration($email: String!, $slug: String!) {
  eventRegistration(email: $email, slug: $slug) {
    activationState
    event {
      id
      title
    }
    isClearedForSignup
    registrant {
      id
      email
    }
  }
}
